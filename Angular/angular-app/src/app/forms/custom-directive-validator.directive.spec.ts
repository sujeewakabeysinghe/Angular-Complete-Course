import { CustomDirectiveValidatorDirective } from './custom-directive-validator.directive';

describe('CustomDirectiveValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomDirectiveValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
